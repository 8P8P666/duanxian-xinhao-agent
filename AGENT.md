# AGENT.md —— 给评委的速览

> 这份文件是「短线共振信号智能体」的 Agent 形态说明。
> 想直接跑起来看结果：见 README.md 的用法一节。

---

## ⭐ 30 秒看懂：这个作品用了币安官方的什么

一句话：**用币安官方的 Skills Hub 和官方开源资源拿数据，用我们自己写的规则引擎出结论。**

具体是三件事：

1. **官方技能 `binance`** —— 用它驱动的官方命令行工具 `binance-cli`，
   按官方 SKILL.md 里记载的 `binance-cli request GET <url>` 用法直接调官方公开入口取 K 线；
2. **官方公开行情接口 `data-api.binance.vision`** —— 免密钥的 K 线接口，一次请求取回行情；
3. **官方开源数据仓库 `data.binance.vision`** —— 历史 K 线归档 ZIP，可复现、可复核。

而「四个指标怎么投票、权重怎么定、哪些信号该被过滤掉、评级怎么分档」这套算法，
是**本项目自己写的**（`indicators.js` + `signal-engine.js`），官方并没有提供这类合成结论 ——
这部分才是作品的价值所在。

> **话术说明**：我们不说「基于官方 skill 开发」，因为那不准确。
> 准确的说法是 **数据层用官方资源，算法层和结论层自研** —— 后两层官方都没有现成的。

---

## 一、实际用到的官方资源（逐项核对）

| # | 官方资源 | 用在哪 | 可核验的证据（均为实测） |
|---|---|---|---|
| 1 | 官方技能 `binance`（币安 Skills Hub） | `--skill` 通道：经官方 CLI 的 `request` 子命令调官方公开入口 | 实测 `via = binance-cli`，**官方 CLI 确实被调用**（不是回退路径）；取回 500 根K线，端到端 **1.43 秒** |
| 2 | 官方公开行情接口 `data-api.binance.vision` | `--live` 通道：K 线 + 24 小时快照 | 实测取回 **500 根K线**，端到端 **0.74 秒** |
| 3 | 官方开源数据仓库 `data.binance.vision` | `--official` 通道：`data/{spot\|futures/um}/daily/klines/<交易对>/<周期>/…zip` | 实测 3 天归档共取回 **72 根 1 小时K线**，端到端 **0.65 秒**，归档日期 2026-09-10 ~ 2026-09-12 如实标注 |

**网页端用的是同一套引擎**：`data-direct.js` 与 `agent.mjs` 加载的是同一份
`indicators.js` + `signal-engine.js`，两边结论口径完全一致。

---

## 二、官方 Skills Hub 全量核对：为什么只用这几项

币安官方技能市场（`binance/binance-skills-hub`）共 **20 个技能**，分两组。
我们逐个核对过**能不能真跑**，不做挂名：

### binance 组（7 个）

| 技能 | 结论 |
|---|---|
| `binance` | ✅ **在用** —— 本作品经它驱动的官方 CLI 取数 |
| `academy-skill` / `fiat` / `square-post` | ❌ 走 `www.binance.com` 域，实测连接被隧道拒绝（502） |
| `p2p` | ❌ 走 `c2c.binance.com`，不可达 |
| `payment` / `onchain-pay` | ❌ 域名可连但**必须自备 API 密钥并签名**，本作品是纯公开数据作品，不需要 |

### binance-web3 组（13 个）

这组里带命令行脚本的技能，主力接口都在 `web3.binance.com` 下，实测是
**SNI 层阻断**（用公共 DoH 取到真实 IP 后直连，正确 SNI 会被 40~50 毫秒内 `ECONNRESET`），
换 DNS、改 hosts 都救不回来。

**要特别说清楚的一点**：这组里确实有和本作品**功能相近**的技能 ——
`trading-signal` 与 `binance-trading-signal` 本身就是「交易信号」类技能。

但我们**不是**因为「官方没有这个能力」才自研，而是两点口径差异：

1. **接口不可达**：这两个技能的接口挂在 `web3.binance.com`，实测 SNI 阻断，装了也取不到数；
2. **口径不同**：它们输出的是官方模型给出的信号判定，属**综合信号口径**；
   本作品是**四个经典技术指标（MACD / RSI / KDJ / BOLL）在中心化K线上的加权投票 +
   自研假信号过滤**，属**可逐项复核的规则口径**。两者互补，不是重复。

另外**不是整组都不可用**：`query-token-info` 的 kline 子命令走的是另一个域
（`dquery.sintral.io`），实测可达；它提供的是链上 K 线，与本作品的中心化 K 线口径不匹配，
所以没有接入。我们没有采用「整组不可达」这种一刀切的说法。

其余未接入的原因也逐项交代：

- `crypto-market-rank`：全市场热度 / 涨幅榜，走 `web3.binance.com`，实测不可达。
- `binance-leaderboard` / `binance-wallet-tracker`：依赖 `binance-agentic-wallet` 的 MCP，走 web3 域，不可达。
- 其余（`meme-rush`、`query-token-audit`、`query-address-info`、`binance-onchain-copy-trader`、
  `binance-tokenized-securities-info`、`binance-sports-ai-analyzer`、`binance-agentic-wallet`）：
  业务领域与本作品不同，或接口不可达。

### 关于官方 connector 仓库

币安官方还提供 connector 类仓库（各语言 SDK 连接器）。本作品**没有引入**，原因是：

1. 它们面向「用 API 密钥交易账户」的场景，本作品不涉及账户、不下单、不碰资金；
2. 它们默认指向 `api.binance.com` / `fapi.binance.com`，本机网络环境实测不可达
   （连接被隧道拒绝），引入会变成一个取不到数的死依赖；
3. 本作品的定位是**公开行情的结构观测工具**，用公开数据域就够了。

---

## 三、价值边界：哪些是官方的，哪些是自研的（三层法）

| 层 | 内容 | 来源 |
|---|---|---|
| **数据层** | K 线、历史归档、24 小时快照 | **币安官方**（能拿官方的一律拿官方） |
| **算法层** | MACD / RSI / KDJ / BOLL 四个指标的实现与参数、加权投票公式、六条假信号过滤规则、四档分档阈值 | **自研**（`indicators.js` + `signal-engine.js`，纯规则、可复现） |
| **结论层** | 共振强度、评级、过滤逐条判定、评级变化轨迹 | **自研**（官方没有这类组合结论） |

要特别说清楚的一点：**是「官方没有这套算法和结论」，不是「官方没有数据」**。
币安官方的数据覆盖非常广（公开行情、历史归档、合约指标等），
本作品的价值在于**把官方数据加工成可逐项复核的结构判定**。

---

## 四、四个数据通道

| 通道 | 数据来源 | 口径 | 实测（BTC 1 小时） |
|---|---|---|---|
| 多平台实时（默认） | 综合交易平台 + 链上永续平台公开行情接口 | 永续/现货 | 500 根 · 1.20 秒 · `via=direct` |
| `--live` | 币安官方公开行情接口 | 现货 | 500 根 · 0.74 秒 · `via=official-rest` |
| `--official` | 币安官方开源数据仓库 | 历史归档（T+1） | 72 根 · 0.65 秒 · `via=official-archive` |
| `--skill` | 官方技能 `binance` + 官方 CLI | 现货 | 500 根 · 1.43 秒 · `via=binance-cli` |

```bash
node agent.mjs                                   # 默认通道（多平台实时）
node agent.mjs ETH 15m                           # 指定币种与周期
node agent.mjs BTC --live                        # 官方公开行情接口
node agent.mjs BTC --official --days 5           # 官方开源数据仓库
node agent.mjs SOL --skill --json                # 官方技能通道 + 纯 JSON
node agent.mjs "用官方开源数据看看 BTC 一小时"      # 自然语言，自动选通道
```

`--json` 时 stdout 是纯 JSON，进度信息全部走 stderr，可以被别的程序直接消费。

> 补充说明：本作品是**按K线即时计算**的横截面分析，每次运行都重新取数重算，
> **没有需要跨次保留的本地状态**，因此不提供 `--status` / `--reset` 这类状态管理参数
> （同系列其他作品有，是因为它们要在本地累积逐笔样本或对比基线）。

---

## 五、诚实边界（必须一起看的说明）

1. **结论与K线根数口径有关**：EMA / MACD / RSI 的取值依赖整段窗口，所以 `--limit` 不同、
   或不同通道取到的根数不同，结论数值会有差异。跨通道比较请保持根数一致，这不是数据出错。
2. **「评级变化轨迹」只算基础分、不含过滤降级**（引擎原有口径），
   它的分数与「共振评级」的最终分不是同一个口径，轨迹用于看方向是否稳定。
3. **官方开源数据仓库是 T+1 归档**，最新一两天通常还没发布。`--official` 从昨天往前探测，
   实际用了哪几天、有没有缺失，都在 `archive` 与 `honesty` 里如实标注，**不补齐、不猜数**。
4. **官方归档存在「面值币」代码**（如 PEPE 实际是 `1000PEPEUSDT`），
   `--official` 会自动尝试倍数前缀，命中后如实标注。
5. **`--live` / `--skill` 是现货口径**，不含合约持仓量与资金费率，这两项如实留空，不估算。
6. **最新一根K线可能仍在形成中**，方向会随价格变化；量能比较已避开这根未收线K线，
   输出里用 `lastCandleForming` 如实标注。
7. **K线不足 60 根时直接如实报错**，不硬算 —— EMA50 与 MACD 需要足够的起步长度。
8. **结果仅用于研究，不构成投资建议**；评级是程序化技术信号，不等于交易指令。

---

## 六、与已提交参赛说明的关系

本项目已按「网页作品」的形式提交过一版说明。这次新增的是 **Agent / 技能形态**：

- **网页形态**（`index.html` + `app.js` + `functions/`）继续保留，功能和口径都不变；
- **新增加的**是命令行入口 `agent.mjs` 与技能定义 `skills/duanxian-gongzhen/SKILL.md`，
  让这个作品可以被技能安装器装载、被其他 Agent 直接调用；
- 两种形态**共用同一套自研引擎**（`indicators.js` + `signal-engine.js`），
  结论口径完全一致，不是两个不同的东西 —— `agent.mjs` 用 `new Function` 直接执行网页的引擎文件，
  引擎一行都没有改。
