# 短线共振信号智能体 · 项目说明

**参赛标识：币安 Agent 参赛作品 · 方向：数据分析**

**项目仓库：duanxian-xinhao-agent**

---

## 一、一句话介绍

同时计算 MACD、RSI、KDJ、BOLL 四个技术指标，判断它们是否在同一方向上形成共振，
并对容易失效的单一指标信号进行二次过滤，最终输出四档市场结构评级。

> 四指标同步确认，过滤单一信号噪音。
> 不是看到一个金叉就行动，而是等待多个指标形成共振。

---

## 二、项目背景：真正的问题不在「找信号」，而在「筛信号」

技术指标最常见的用法是「看到金叉就做多、看到死叉就做空」。但在实际行情中，
单一指标会频繁产生无效交叉：

- 震荡行情里，MACD 和 KDJ 会来回反复交叉，一天能出现好几次；
- 趋势末端，RSI 会在超买或超卖区长期钝化，指标数值失去参考意义；
- 成交量萎缩时的价格波动，往往只是流动性不足造成的噪音，并不是真实方向；
- 逆着中期趋势的短线反弹，很容易做出「看起来成立、实际站不住」的信号。

因此本项目解决的核心问题不是「再发明一个指标」——市面上指标已经足够多——
而是 **在多个指标之间建立交叉确认机制，并主动识别、降级那些可能失效的信号**。

它更重要的能力是 **判断什么时候不该行动**。

---

## 三、核心功能

### 功能一：多指标共振判定

四个指标各自独立给出方向判断（偏多 / 偏空 / 中性），再按权重汇总为综合评分。

| 指标 | 参数 | 权重 | 方向判定依据 |
|---|---|---|---|
| MACD | 12 / 26 / 9 | 1.2 | DIF 与 DEA 的相对位置，并识别金叉 / 死叉 |
| RSI | 14（Wilder 平滑） | 0.9 | 55 以上偏多、45 以下偏空，并标注超买超卖 |
| KDJ | 9 / 3 / 3 | 1.0 | K 线与 D 线的相对位置，并识别金叉 / 死叉 |
| BOLL | 20 周期 / 2 倍标准差 | 1.0 | 收盘价相对中轨的位置，并计算通道位置与带宽 |

权重不是随意设定的：MACD 与 KDJ 属于方向型指标，对趋势转折更敏感，因此权重较高；
RSI 属于区间型指标，在震荡中容易钝化，因此权重略低。

综合评分区间为 **-100 到 +100**，映射为四档评级：

| 评级 | 评分区间 | 含义 |
|---|---|---|
| 强做多 | ≥ +55 | 多个指标方向一致向上，且通过主要过滤规则 |
| 谨慎做多 | +25 ~ +55 | 偏多指标占多数，但仍有关键条件未满足 |
| 震荡 | -25 ~ +25 | 多空接近，或信号被过滤规则完全削弱 |
| 强做空 | ≤ -25 | 多个指标方向一致向下，且通过主要过滤规则 |

> 评级属于程序化技术信号，用于描述市场结构，不是交易指令。

### 功能二：假信号过滤

六条过滤规则，每条独立判断，触发即按权重扣减信号强度。
每条规则在页面上都会显示 **通过 / 已触发** 以及 **具体触发原因**，而不是只给一个笼统结论。

| 规则 | 触发条件 | 扣分 | 过滤目的 |
|---|---|---|---|
| 孤立信号检查 | 四个指标中只有 1 个给出方向 | -22 | 排除典型的单一指标信号 |
| 量能确认 | 已收线K线成交量低于 20 根均量的 80% | -14 | 排除缺少资金参与的价格变化 |
| 趋势一致性 | 信号方向与 EMA20 / EMA50 中期趋势相反 | -16 | 排除逆势的短线反弹或回落 |
| 极端值风险 | 偏多时 RSI ≥ 75 或价格超出布林上轨；偏空时反向同理 | -20 | 排除追涨杀跌位置 |
| 通道收窄检查 | 布林带宽处于近 60 根的偏低水平 | -12 | 波动压缩时方向尚未选择 |
| K线方向确认 | 最新K线方向与信号方向不一致 | -8 | 要求信号得到K线配合 |

**两条关键的计算约束（这是本项目在实现上最需要讲清楚的地方）：**

1. **过滤只能削弱信号，不能加强，也不能反转方向。**
   计算方式是「以多指标投票得分为基础分，过滤规则只做向下调整，且调整后不允许跨越 0 轴」。
   因此页面不会出现「因为触发了降级规则，评级反而变强」这类自相矛盾的结果。
2. **成交量比较只使用已经收线的K线。**
   正在形成中的K线成交量天然不完整（例如 15 分钟周期刚走 2 分钟，成交量只有正常水平的一小部分），
   如果直接用它去和 20 根均量比较，会导致几乎每一根K线都被误判成缩量。

---

## 四、技术实现

### 4.1 整体架构

采用 **静态前端 + 双通道取数** 的架构，既能连接 Netlify 仓库部署，也能纯静态直接上线：

```text
                    浏览器页面（仓库根目录）
                          │
                ┌─────────┴─────────┐
                │  通道一（优先）    │  通道二（自动兜底）
                ▼                   ▼
        /api/market、/api/symbols   直连交易所公开接口
        Netlify Functions                │
                │                        │
                └────────────┬───────────┘
                             ▼
             公开行情接口（综合平台 / 链上平台）
```

**通道一（云端接口代理）**：页面统一请求 `/api/xxx`，由 Netlify Functions 转发到交易所。
优点是统一处理跨域与超时、带服务端短时缓存（行情 6 秒、币种列表 60 秒）、
后续更换数据源只需改函数不必动页面。

**通道二（浏览器直连兜底）**：当页面上没有部署云端函数时
（例如把静态文件直接拖到 Netlify Drop，或放在任意静态空间），
页面会自动检测并改用浏览器直接访问交易所公开接口。
这样同一套页面代码既能跑在仓库部署环境，也能跑在纯静态环境，不会白屏。

**两条通道的取舍说明：** 交易所公开行情接口本身允许跨域访问，
所以浏览器直连在技术上完全可行。保留云端代理是因为它多了服务端缓存与统一的错误处理，
在仓库部署方式下体验更好；而直连通道保证了部署方式的自由度。

页面会把当前实际使用的通道如实显示在「数据源」一栏，用户可以看到
「浏览器直连」或「云端接口代理」，不存在混淆。

前端不直接请求交易所接口（在通道一下），而是统一请求 `/api/xxx`。这样做的好处：

- 统一处理跨域问题；
- 统一处理接口超时与错误；
- 后续更换数据源时只需改函数，不必动页面；
- 更适合海外线上访问环境。

### 4.2 技术选型说明

| 部分 | 实现方式 | 原因 |
|---|---|---|
| 指标计算 | 纯 JavaScript 前端计算 | 四个指标全部根据接口返回的真实K线逐根计算，不使用任何预置数值 |
| K线图表 | 原生 Canvas 手绘 | 不加载任何外部图表库，避免 CDN 加载失败导致页面空白 |
| 取数通道 | 云端函数优先，浏览器直连兜底 | 兼顾服务端缓存与部署自由度，任一通道不可用时另一条自动接管 |
| 取数口径 | 云端与浏览器两版代码口径完全一致 | 两条通道返回的数据结构与换算方式相同，结果不会因通道不同而变化 |
| 本地调试 | 自建 dev-server | 复用与线上完全相同的接口代码，保证本地验证结果与线上一致 |

### 4.3 已解决的真实缺陷

开发过程中的自检（用真实行情数据回放、并在模拟的纯静态环境里跑完整页面）
发现并修复了以下问题：

| 缺陷 | 现象 | 修复方式 |
|---|---|---|
| 指标引擎未传出K线数据 | 信号引擎读取K线时报错，页面白屏 | 在指标计算结果中补上K线数组 |
| 量能过滤误判 | 用未收线K线对比20根均量，几乎每根都被判缩量 | 改为只使用已收线K线参与比较 |
| 过滤扣分导致方向反转 | 出现「只有 2/4 指标共振，却判定强做空」的自相矛盾结果 | 过滤改为只做向下调整，且不允许跨越 0 轴 |
| 交易所英文错误码直接暴露 | 币种不存在时页面显示 `CONTRACT_NOT_FOUND` 之类的英文码 | 增加错误翻译层，转为「该合约在交易所不存在」等中文提示 |
| 错误标题固定 | 错误框标题写死为「数据获取失败」，渲染出错时误导用户去查网络 | 取数与渲染分成两个阶段，错误标题随实际环节变化 |
| 无画布环境导致整页失败 | 浏览器拿不到绘图上下文时，整页分析结果都无法显示 | 图表增加不可用降级：隐藏画布、给出中文说明，其余结果照常显示 |
| 图表提示文字被覆盖 | 画布不可用的说明刚写入就被后一行代码覆盖掉 | 增加条件判断，画布不可用时不再覆盖说明文字 |

---

## 五、数据来源与合规说明

| 项目 | 说明 |
|---|---|
| 主数据源 | **综合交易平台公开行情接口**（现货 / USDT 永续合约） |
| 备用与增强数据源 | **链上永续平台公开行情接口**（链上永续合约） |
| 跨平台对照 | 同一币种在两个平台的公开行情价格、持仓量、资金费率同时抓取并计算价差 |
| 是否需要密钥 | **不需要**。两个接口都是公开行情接口 |
| 是否涉及交易 | **不涉及**。不接入交易权限，不保存私钥，不执行任何下单 |
| 是否涉及账户 | **不涉及**。不登录、不绑定、不读取任何账户信息 |

**资金费率口径处理：**
链上永续平台资金费率按 1 小时结算，综合交易平台按 8 小时结算，两者不能直接比较。
页面统一换算成 **年化资金费率** 后再展示，同时保留各自的原始结算周期标注，
避免出现「看起来一边高一边低，其实是结算周期不同」的误读。

**数据真实性原则：**
页面只展示接口真实返回的数据。当接口失败或超时时，页面显示明确的失败原因，
**不会用演示数据或固定数值顶替，也不会把演示数据描述成实时数据**。

---

## 六、页面结构

采用多列流式仪表盘布局，自上而下分为 12 个区块：

1. **顶部品牌栏** —— 币安 Agent 参赛作品 · 方向：数据分析，右侧显示数据源连接状态
2. **主视觉区** —— 项目名称、宣传语、接入数据源与输出说明，配 Web3 节点网络装饰
3. **分析控制台** —— 平台类型（综合平台 / 链上平台）、市场类型（现货 / 永续）、分析币种、分析周期、开始分析
4. **行情摘要卡片** —— 最新价、24 小时涨跌、24 小时成交额、年化资金费率、持仓量、标记价、布林位置、共振评价
5. **共振结论卡片** —— 评级徽章、综合评分条、共振强度、投票明细、过滤扣分、结论说明
6. **假信号过滤结果** —— 六条规则逐条列出通过或触发状态及具体原因
7. **四指标逐项拆解** —— 四张指标卡，各自显示数值、方向与标签
8. **价格结构与布林通道** —— 自绘K线图，含布林三轨、成交量柱与鼠标定位
9. **评级变化轨迹** —— 近 48 根K线的评级色带，用于观察方向判断是否稳定
10. **跨平台真实数据对照** —— 两平台价格、价差、持仓量、年化资金费率
11. **数据来源与计算口径说明**
12. **风险提示**

**视觉风格：**
黄色数据分析台风格（浅黄底 + 米白卡片 + 黑色文字 + 深橙强调色），
加入 BTC 货币标识、区块链节点连线、数据网格等 Web3 元素。
按中国习惯，做多与上涨使用红色，做空与下跌使用绿色。

明确不做自动交易机器人的视觉暗示：不出现下单按钮、不出现伪造订单、不出现「一键跟单」类表达。

---

## 七、使用方法

### 7.1 在线使用

打开部署地址后：

1. 在「分析控制台」选择平台类型：**综合平台**（推荐，覆盖更广）或 **链上平台**
2. 选择市场类型：**现货** 或 **永续合约**
3. 选择币种：BTC、ETH、SOL 等（永续合约下可选币种更多）
4. 选择分析周期：5分钟 / 15分钟 / 1小时 / 4小时 / 1天
5. 点击「开始分析」

页面会自动完成：拉取K线 → 计算四指标 → 投票共振 → 执行六条过滤 → 输出评级。
默认参数为 **综合平台 · 永续合约 · BTC · 15分钟**，打开即可直接看到结果。

### 7.2 本地运行

需要先安装 Node.js（18 或更高版本）。

```bash
cd "D:/币安作品8p"
node dev-server.mjs
```

启动后在浏览器打开：

```text
http://127.0.0.1:8790
```

---

## 八、目录结构

```text
币安作品8p/
├─ index.html                   # 页面结构
├─ styles.css                   # 黄色主题与仪表盘样式
├─ favicon.svg
├─ indicators.js                # MACD / RSI / KDJ / BOLL 计算
├─ signal-engine.js             # 共振判定与假信号过滤
├─ chart.js                     # 自绘K线图表（无外部依赖）
├─ data-direct.js               # 浏览器直连取数（云端函数不可用时自动兜底）
├─ api-client.js                # 接口调用封装与通道自动选择
├─ app.js                       # 页面主逻辑与渲染
├─ functions/                   # 云端接口代理（Netlify Functions）
│  ├─ _data.cjs                 # 两个平台的行情抓取与统一格式
│  ├─ market.cjs                # /api/market  行情与K线
│  └─ symbols.cjs               # /api/symbols 币种列表
├─ netlify.toml                 # Netlify 构建与路由配置
├─ package.json
├─ dev-server.mjs               # 本地测试服务器
├─ 打包上传文件夹.mjs           # 生成两个可直接拖拽上传的文件夹
├─ 检查上传格式.mjs             # 上传前格式体检
├─ README.md                    # 技术文档
├─ 项目说明.md                  # 本文档
├─ 上传到GitHub-操作步骤.md     # GitHub 上传指引
└─ 上传到Netlify-操作步骤.md    # Netlify 部署指引
```

**关于扁平结构：** 网页文件全部直接位于仓库根目录，整个项目只有一个 `functions/`
文件夹。这样设计的原因很实际 —— GitHub 网页版上传只接受单独的**文件**，
选中文件夹拖进去会被忽略。扁平结构下按 `Ctrl+A` 全选出来的全是文件，
上传可以一次完成，避免因目录层级过深导致漏传文件。


---

## 九、项目亮点总结（评审视角）

1. **方向选择有差异化** —— 市场主流做法是「叠加更多指标找信号」，
   本项目反过来做「多指标交叉确认 + 主动过滤无效信号」，解决的是同一问题的另一面。
2. **过滤逻辑可解释** —— 六条过滤规则全部在页面上显示触发原因和扣分数值，
   不是黑盒打分，用户可以清楚知道「为什么结论被降级」。
3. **计算约束经过推敲** —— 明确处理了「过滤不得加强信号」「成交量只取已收线K线」
   这两个容易被忽略但会直接影响结果正确性的细节。
4. **数据可追溯** —— 主数据源、备用数据源、跨平台价差全部标注来源与更新时间，
   接口失败时如实报错，不伪造实时状态。
5. **零外部依赖** —— 图表自绘、指标自算、无 CDN，页面加载失败风险低。
6. **部署合规** —— 不接入交易权限、不保存私钥、不自动下单，定位为数据分析工具而非交易机器人。
7. **部署方式自由** —— 同一套页面代码，既能配合云端函数部署（带服务端缓存），
   也能纯静态直接上线（自动切换浏览器直连），不会因为部署方式受限而白屏。
8. **真实环境验证** —— 除了用真实行情回放指标逻辑，还在模拟的纯静态服务器上
   完整跑过页面（含无画布等极端情况），验证结果不是只看代码得出的结论。

---

## 十、风险提示

本页面仅用于数字资产市场风险观测与数据分析，**不构成投资建议**，不代表任何买卖承诺。
页面中的「强做多」「强做空」等评级是程序化技术信号的计算结果，**不等于实际交易建议**。

技术指标是对历史价格的统计描述，天然滞后；震荡行情中指标容易反复交叉，趋势末端指标容易钝化。
本项目的过滤规则只能降低部分噪音，无法消除判断误差；行情数据可能存在延迟、缺失或接口波动。

市场有风险，投资需谨慎。任何投资决策应结合个人风险承受能力、资金状况和投资目标独立判断，
必要时咨询持牌专业机构。过往表现不预示未来收益。
